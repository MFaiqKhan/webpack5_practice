function generateJoke() {
    return "What do you call a fake noodle?" + "\n" + "An Impasta!";
};

export default generateJoke; // export default is a way to export a function, something like we do in react 
// this won't work if we don't have module bundler installed in the package
